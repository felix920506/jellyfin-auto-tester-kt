# Stage 1: Analysis

Owns issue analysis and emits `ReproductionPlan Markdown v1` messages to
`plan_ready` for standard Stage 2 execution or `web_client_plan_ready` for pure
Jellyfin Web client plans.

See `plans/stage1-analysis/plan.md` for the detailed implementation plan.
